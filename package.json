{
  "name": "wilmak",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w player-web && npm run build:server -w desktop && concurrently -k -n SRV,APP -c yellow,magenta \"npm run watch:server -w desktop\" \"npm run dev -w desktop\"",
    "dev:gm": "npm run dev -w desktop",
    "dev:player": "npm run dev -w player-web",
    "build:player": "npm run build -w player-web",
    "sync:game-data": "python3 scripts/sync-game-data.py",
    "dist": "npm run build -w desktop && npm run build:server -w desktop && npm run build -w player-web && npm run pack -w desktop",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "concurrently": "^9.0.1",
    "typescript": "^5.5.4",
    "wait-on": "^8.0.1"
  }
}
