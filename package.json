{
  "name": "wilmak",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run build -w player-web && concurrently -k -n GM,ELECTRON -c blue,magenta \"npm run dev -w desktop\" \"wait-on tcp:5173 && npm run electron -w desktop\"",
    "dev:gm": "npm run dev -w desktop",
    "dev:player": "npm run dev -w player-web",
    "electron": "npm run electron -w desktop",
    "build:player": "npm run build -w player-web",
    "dist": "npm run build -w desktop && npm run build:server -w desktop && npm run build -w player-web && npm run pack -w desktop"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "wait-on": "^8.0.1"
  }
}
